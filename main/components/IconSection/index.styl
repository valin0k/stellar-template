_gray = #dddddd
_darkGray = #636363
_fontWeight = 200

.root
  align-items center

.title
  font-size font(xxl)
  font-weight _fontWeight
  color _darkGray

.titleWrapper
  align-items center

.content
  align-items center

.section
  align-items center
  margin-bottom gutter(l)

.iconBorderWrapper
  padding gutter(s)
  border 1px solid _gray
  border-radius 50%
  margin-bottom gutter(l)

.iconWrapper
  border-radius 50%
  border 1px solid _gray
  padding gutter(xl)

.itemText
  text-align center
  font-weight _fontWeight
  font-size font(l)
  line-height 25px

.itemTitle
  text-align center
  font-weight _fontWeight
  font-size font(xl)
  margin-bottom gutter(l)
